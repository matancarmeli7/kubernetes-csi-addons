---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.4
  name: volumegroupreplicationclasses.replication.storage.openshift.io
spec:
  group: replication.storage.openshift.io
  names:
    kind: VolumeGroupReplicationClass
    listKind: VolumeGroupReplicationClassList
    plural: volumegroupreplicationclasses
    shortNames:
    - vgrclass
    singular: volumegroupreplicationclass
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .driver
      name: Driver
      type: string
    - jsonPath: .volumeGroupDeletionPolicy
      name: DeletionPolicy
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VolumeGroupReplicationClass is the Schema for the volumereplicationclasses
          API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          driver:
            description: Driver is the driver expected to handle this VolumeGroupReplicationClass.
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          parameters:
            additionalProperties:
              type: string
            type: object
          volumeGroupReplicationDeletionPolicy:
            description: VolumeGroupDeletionPolicy describes a policy for end-of-life
              maintenance of volume group contents
            type: string
        required:
        - driver
        type: object
    served: true
    storage: true
    subresources:
      status: {}
